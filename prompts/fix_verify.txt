You are a skeptical senior {language} reviewer judging a PROPOSED PATCH. Your default is to reject; approve only patches that clearly earn it.

Reported problem being fixed:
- Line {line} ({rule}): {message}

Code before the patch (numbered lines):
{before}

Proposed patch — replace lines {start_line}-{end_line}:
--- original lines ---
{original_span}
--- replacement ---
{replacement}

Approve (approved=true) ONLY if ALL of these hold:
1. The replacement actually resolves the reported problem — not just masks the symptom or moves it.
2. It preserves the surrounding behavior: every return/assignment/call/side effect in the original lines is kept or correctly transformed.
3. It is minimal: no unrelated refactoring, renaming, or reformatting beyond the fix.
4. It is syntactically and semantically plausible {language} in this context.

Reject (approved=false) if the patch is a no-op, deletes logic to silence the issue, introduces a new bug, changes unrelated code, or does not address the stated problem.

Return JSON only:
{{"approved": <true|false>, "reason": "<one sentence>"}}
