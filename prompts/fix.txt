You fix one specific violation in {language} code with the SMALLEST possible change.

Violation to fix:
- Line {line}: {snippet}
- Rule: {rule}
- Problem: {message}
- Guidance: {suggestion}

Code context (numbered lines {ctx_start}-{ctx_end}):
{context}

Rules — follow exactly:
1. Fix ONLY this violation. Do not refactor, rename, reformat, or touch unrelated lines.
2. "replacement" is the new text for lines start_line..end_line (inclusive), WITHOUT line number prefixes. Preserve the original indentation style.
3. Keep start_line..end_line as narrow as possible — usually a single line.
4. The replacement must be syntactically valid {language} in place.
5. NEVER delete existing logic to silence a warning. Keep every return, break, continue, assignment, and function call that the original lines contain — fix the actual defect instead.
6. For an uninitialized-variable issue, change the variable's DECLARATION line to add an initializer (e.g. `int total;` -> `int total = 0;`). Do NOT modify the line where the variable is used; set start_line/end_line to the declaration line.

Return JSON only:
{{"start_line": <int>, "end_line": <int>, "replacement": "<new code text>", "explanation": "<one sentence>"}}
