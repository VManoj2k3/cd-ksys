# Optional: HuggingFace token (only needed for gated models)
HF_TOKEN=
# Optional: override config.yaml location (replaces it entirely)
KOOSYS_CONFIG=config.yaml
# Optional: extra config file deep-merged OVER the base (deployment deltas)
KOOSYS_CONFIG_OVERLAY=
# Optional: ngrok auth token if tunnel.provider=ngrok
NGROK_AUTHTOKEN=
