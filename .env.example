# Optional: HuggingFace token (only needed for gated models)
HF_TOKEN=
# Optional: override config.yaml location
KOOSYS_CONFIG=config.yaml
# Optional: ngrok auth token if tunnel.provider=ngrok
NGROK_AUTHTOKEN=
